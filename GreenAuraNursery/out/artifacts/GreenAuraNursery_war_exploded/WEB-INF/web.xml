<?xml version="1.0" encoding="UTF-8"?>
<!--
  ============================================================
  web.xml — Web Application Deployment Descriptor
  ============================================================
  This file tells the server (Tomcat) how to set up the app.
  It defines:
    - The welcome page (what page loads when you visit /)
    - Session timeout settings
    - Error pages (optional)

  NOTE: Since we use @WebServlet annotations in our Java files,
  we do NOT need to register each servlet here.
  The @WebServlet("/register") annotation does that automatically.
  ============================================================
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- App display name -->
    <display-name>Green Aura Nursery</display-name>

    <!-- Welcome page: when user visits http://localhost:8080/GreenAuraNursery/ -->
    <!-- Tomcat will forward to /home which is handled by HomeServlet -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Session timeout: 30 minutes of inactivity logs the user out -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
